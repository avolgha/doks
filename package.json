{
  "name": "doks",
  "license": "BSD-3-Clause",
  "private": true,
  "devDependencies": {
    "typescript": "^5.5.3"
  },
  "scripts": {
    "build": "pnpm run -r build",
    "lint": "pnpm run -r lint",
    "start:client": "pnpm run -C packages/doks-client watch",
    "start:server": "pnpm run -C packages/doks-server dev"
  }
}
